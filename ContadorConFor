#include <iostream>
using namespace std;
int main()
{
    int veces, i;
    cout<<"Ingrese la cantidad de veces que quiere que se repita el programa: ";
    cin>>veces;
    for (i = 1; i <= veces; i++) {
        cout<<"El valor del contador es: "<<i<<endl;
    } 
    return 0;
}
