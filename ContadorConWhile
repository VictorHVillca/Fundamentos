#include <iostream>
using namespace std;
int main()
{
    int contador, veces;
    cout<<"Ingrese la cantidad de veces que quiere que se repita el programa: ";
    cin>>veces;
    contador = 0;
    while (contador<veces) {
        contador = contador + 1;
    } 
    cout<<"El valor del contador es: "<<contador;
    return 0;
}
